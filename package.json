{
  "dependencies": {
    "mintlify": "^4.2.87",
    "yaml": "^2.8.1"
  },
  "scripts": {
    "dev": "node scripts/pull.js && mint dev",
    "build": "mint build",
    "start": "mint dev",
    "pull": "node scripts/pull.js"
  }
}
